export class Food {
    public key: string;
    public description: string;
    public value: string;
    public visible: boolean;
    public include: string[] = [];
    public exclude: string[] = [];
}
